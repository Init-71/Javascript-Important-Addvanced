// Configuración
const myArray = [["John", 23], ["cat", 2]];

// Cambia solo el código debajo de esta línea
//pop() se ocupa para eliminar el ultimo elemento del array
var removedFromMyArray = myArray.pop();